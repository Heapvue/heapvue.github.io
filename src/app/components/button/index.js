import React from "react";

export const Button = ({ text }) => {
  return <button className="btnP">{text}</button>;
};

export const ButtonSecondary = ({ text }) => {
  return <button className="btnS">{text}</button>;
};
